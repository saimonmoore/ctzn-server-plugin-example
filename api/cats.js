/*
 * You generally won't need this since you can use the `table` and `view` api calls
 *
 * ...but if you need to you can!
 */
export const setup = function setup(wsServer, config) {
  // wsServer.register('cats.doAdvancedCatThings', async ([userId, opts], client) => {
  //   ...
  // })
  console.log('[ctzn-server-plugin-example] Setup advanced api extensions!')
}
